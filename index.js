'use strict';

const DATABASES = require('./lib/databases');
const MODEL = require('./lib/model');
const QUERY = require('./lib/query');
const SCHEMA = require('./lib/schema');
const SCHEMA_FIELD = require('./lib/schemaField');

module.exports = {
  DATABASES,
  MODEL,
  QUERY,
  SCHEMA,
  SCHEMA_FIELD,
};
